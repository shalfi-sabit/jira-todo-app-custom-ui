import styled from "styled-components";

const Status = styled.span`
  float: right;
  display: inline-flex;
  align-items: center;
  margin-top: -24px;

  & > span {
    margin-left: 8px;
  }
`;

export default Status;
